#include <iostream>
#include <cstdio>
using namespace std;
int digit_sum(long long int num);
int main(){
    long long int  i,sum;
    while(cin>>i && i!=0){

        sum=digit_sum(i);
        cout<<sum<<endl;
    }
}

int digit_sum(long long int num){
    long long int sum = 0;
    while (num > 0){
        sum += num % 10;
        num /= 10;
    }
    if (sum > 9){
        sum = digit_sum(sum);
    }
        return sum;
}
